# `turbo_decoder_top` Package

This folder contains the active folded-top export package for:

- [`rtl/turbo_decoder_top.vhd`](../../rtl/turbo_decoder_top.vhd)

This package currently focuses on the top module's worthy cell-level
architectural blocks rather than another broad high-level overview.

Main contents:

- [`cell_level/`](cell_level/)
